import { makeStyles } from '@material-ui/core/styles';


export const useStyles = makeStyles(theme => ({
  list: {
    flex: '1 1 auto',
  },
  actionContainer: {
    marginTop: '1rem',
    textAlign: 'center',
  },
  empty: {
    padding: '2rem 0',
  }
}));
